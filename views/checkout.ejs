<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Checkout | AHMED Store</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body.dark-mode {
      background-color: #181a1b !important;
      color: #f1f1f1 !important;
    }
    .dark-mode .card {
      background-color: #23272b !important;
      color: #f1f1f1 !important;
    }
    .dark-mode .navbar, .dark-mode .footer, .dark-mode .bg-dark {
      background-color: #181a1b !important;
    }
    .checkout-card {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 2px 12px rgba(44,62,80,0.08);
      background: var(--color-card-bg, #fff);
      padding: 2rem 2rem 1.5rem 2rem;
    }
    .checkout-title {
      color: #3498db;
      font-weight: bold;
      letter-spacing: 1px;
    }
    .btn-success {
      border-radius: 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      letter-spacing: 1px;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">
  <!-- Responsive Navbar -->
  <%- include('partials/navbar') %>
  <div style="height:70px;"></div>
  <div class="container mt-4 flex-grow-1">
    <div class="row justify-content-center">
      <div class="col-md-8 col-lg-6">
        <div class="card checkout-card">
          <h2 class="text-center mb-4 checkout-title"><i class="bi bi-credit-card"></i> Checkout</h2>
          <form>
            <div class="mb-3">
              <label for="name" class="form-label">Full Name</label>
              <input type="text" class="form-control" id="name" placeholder="Enter your full name" required>
            </div>
            <div class="mb-3">
              <label for="address" class="form-label">Address</label>
              <input type="text" class="form-control" id="address" placeholder="Enter your address" required>
            </div>
            <div class="mb-3">
              <label for="card" class="form-label">Credit Card</label>
              <input type="text" class="form-control" id="card" placeholder="Card number" required>
            </div>
            <button type="submit" class="btn btn-success w-100"><i class="bi bi-bag-check"></i> Place Order</button>
          </form>
        </div>
      </div>
    </div>
  </div>
  <footer class="bg-dark text-white text-center py-3 mt-auto">
    &copy; <%= new Date().getFullYear() %> AHMED Store. All rights reserved.
  </footer>
  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>