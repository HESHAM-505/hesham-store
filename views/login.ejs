<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login - HESHAM Store</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .login-card {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 2px 12px rgba(44,62,80,0.08);
      background: var(--color-card-bg, #fff);
      padding: 2rem 2rem 1.5rem 2rem;
      margin-bottom: 2rem;
    }
    .login-title {
      color: #3498db;
      font-weight: bold;
      letter-spacing: 1px;
    }
    .form-label {
      color: #3498db;
      font-weight: 500;
    }
    .btn-primary {
      border-radius: 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      letter-spacing: 1px;
    }
    .register-link {
      display: block;
      text-align: center;
      margin-top: 1rem;
    }
  </style>
</head>
<body class="d-flex flex-column min-vh-100">

  <!-- Responsive Navbar -->
  <%- include('partials/navbar') %>
  <div style="height:70px;"></div>
  
  <div class="container mt-4 flex-grow-1" style="max-width: 400px;">
    <div class="card login-card">
      <h2 class="text-center mb-4 login-title"><i class="bi bi-box-arrow-in-right"></i> Login</h2>
      <form action="/login" method="POST">
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input type="email" id="email" name="email" class="form-control" placeholder="Enter your email" required>
        </div>
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input type="password" id="password" name="password" class="form-control" placeholder="Enter your password" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Login</button>
      </form>
      <a href="/register" class="register-link">Don't have an account? Register now</a>
    </div>
  </div>

  <footer class="bg-dark text-white text-center py-3 mt-auto">
    &copy; 2025 HESHAM Store
  </footer>

  <!-- Scripts -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/main.js"></script>
</body>
</html>
